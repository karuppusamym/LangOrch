fastapi>=0.115
uvicorn[standard]>=0.30
sqlalchemy>=2.0
aiosqlite>=0.20
pydantic>=2.0
pydantic-settings>=2.0
langgraph>=0.3
langgraph-checkpoint-sqlite>=3.0.0
langchain-core>=0.3
sse-starlette>=2.0
httpx>=0.27
apscheduler>=3.10
croniter>=2.0

# ── Authentication ──────────────────────────────────────────────
PyJWT>=2.8                         # JWT token creation and verification
bcrypt>=4.0                        # Password hashing
passlib[bcrypt]>=1.7               # Password hashing (fallback)

# ── OpenTelemetry observability ────────────────────────────────
opentelemetry-api>=1.27.0
opentelemetry-sdk>=1.27.0
opentelemetry-instrumentation-fastapi>=0.48b0
opentelemetry-exporter-otlp>=1.27.0
python-json-logger>=2.0.7

# ── Database migrations ─────────────────────────────────────────
alembic>=1.13

# ── PostgreSQL async driver (production DB) ────────────────────
asyncpg>=0.29

# ── PostgreSQL LangGraph checkpointer (production) ─────────────
# Install for production; SQLite checkpointer is used in dev by default
# langgraph-checkpoint-postgres>=2.0

# ── Optional secrets providers (install only what you use) ─────
hvac>=2.0                          # HashiCorp Vault
boto3>=1.34                        # AWS Secrets Manager
azure-keyvault-secrets>=4.8        # Azure Key Vault
azure-identity>=1.16               # Azure authentication (DefaultAzureCredential)
